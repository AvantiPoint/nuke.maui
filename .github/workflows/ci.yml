# ------------------------------------------------------------------------------
# <auto-generated>
#
#     This code was generated.
#
#     - To turn off auto-generation set:
#
#         [GitHubWorkflow (AutoGenerate = false)]
#
#     - To trigger manual generation invoke:
#
#         nuke --generate-configuration GitHubActions_ci --host GitHubActions
#
# </auto-generated>
# ------------------------------------------------------------------------------

name: ci

on:
  push:
    branches:
      - master

jobs:
  android-build:
    name: android-build
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v2
        with:
          fetch-depth: 0
      - name: Cache ~/.nuget/packages
        uses: actions/cache@v2
        with:
          path: |
            ~/.nuget/packages
          key: android-build-${{ runner.os }}-${{ hashFiles('**/global.json', '**/*.csproj') }}
      - name: Run './build.cmd CompileAndroid'
        run: ./build.cmd CompileAndroid
        env:
          AndroidKeystoreName: ${{ secrets.ANDROID_KEYSTORE_NAME }}
          AndroidKeystoreB64: ${{ secrets.ANDROID_KEYSTORE_B64 }}
          AndroidKeystorePassword: ${{ secrets.ANDROID_KEYSTORE_PASSWORD }}
      - name: Upload Artifact
        uses: actions/upload-artifact@v3
        with:
          name: android-build
          path: artifacts/android-build
  ios-build:
    name: ios-build
    runs-on: macos-12
    steps:
      - uses: actions/checkout@v2
        with:
          fetch-depth: 0
      - name: 'Setup .NET 6.0.x'
        uses: actions/setup-dotnet@v2
        with:
          dotnet-version: 6.0.x
      - name: Cache ~/.nuget/packages
        uses: actions/cache@v2
        with:
          path: |
            ~/.nuget/packages
          key: ios-build-${{ runner.os }}-${{ hashFiles('**/global.json', '**/*.csproj') }}
      - name: Run './build.sh CompileIos'
        run: ./build.sh CompileIos
        env:
          P12B64: ${{ secrets.P12B64 }}
          P12Password: ${{ secrets.P12PASSWORD }}
          AppleIssuerId: ${{ secrets.APPLE_ISSUER_ID }}
          AppleKeyId: ${{ secrets.APPLE_KEY_ID }}
          AppleAuthKeyP8: ${{ secrets.APPLE_AUTH_KEY_P8 }}
          AppleProfileId: ${{ secrets.IOS_PROVISIONING_PROFILE }}
      - name: Upload Artifact
        uses: actions/upload-artifact@v3
        with:
          name: ios-build
          path: artifacts/ios-build
  maccatalyst-build:
    name: maccatalyst-build
    runs-on: macos-12
    steps:
      - uses: actions/checkout@v2
        with:
          fetch-depth: 0
      - name: 'Setup .NET 6.0.x'
        uses: actions/setup-dotnet@v2
        with:
          dotnet-version: 6.0.x
      - name: Cache ~/.nuget/packages
        uses: actions/cache@v2
        with:
          path: |
            ~/.nuget/packages
          key: maccatalyst-build-${{ runner.os }}-${{ hashFiles('**/global.json', '**/*.csproj') }}
      - name: Run './build.sh CompileMacCatalyst'
        run: ./build.sh CompileMacCatalyst
        env:
          P12B64: ${{ secrets.P12B64 }}
          P12Password: ${{ secrets.P12PASSWORD }}
          AppleIssuerId: ${{ secrets.APPLE_ISSUER_ID }}
          AppleKeyId: ${{ secrets.APPLE_KEY_ID }}
          AppleAuthKeyP8: ${{ secrets.APPLE_AUTH_KEY_P8 }}
          AppleProfileId: ${{ secrets.MACCATALYST_PROVISIONING_PROFILE }}
      - name: Upload Artifact
        uses: actions/upload-artifact@v3
        with:
          name: maccatalyst-build
          path: artifacts/maccatalyst-build
  windows-build:
    name: windows-build
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v2
        with:
          fetch-depth: 0
      - name: Cache ~/.nuget/packages
        uses: actions/cache@v2
        with:
          path: |
            ~/.nuget/packages
          key: windows-build-${{ runner.os }}-${{ hashFiles('**/global.json', '**/*.csproj') }}
      - name: Run './build.cmd CompileWindows'
        run: ./build.cmd CompileWindows
        env:
          AzureKeyVault: ${{ secrets.CODESIGNKEYVAULT }}
          AzureKeyVaultCertificate: ${{ secrets.CODESIGNCERTIFICATE }}
          AzureKeyVaultClientId: ${{ secrets.CODESIGNCLIENTID }}
          AzureKeyVaultClientSecret: ${{ secrets.CODESIGNCLIENTSECRET }}
          AzureKeyVaultTenantId: ${{ secrets.CODESIGNTENANTID }}
      - name: Upload Artifact
        uses: actions/upload-artifact@v3
        with:
          name: windows-build
          path: artifacts/windows-build
  compile-lib:
    name: compile-lib
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v2
        with:
          fetch-depth: 0
      - name: Cache ~/.nuget/packages
        uses: actions/cache@v2
        with:
          path: |
            ~/.nuget/packages
          key: compile-lib-${{ runner.os }}-${{ hashFiles('**/global.json', '**/*.csproj') }}
      - name: Run './build.cmd CompileLib --solution AvantiPoint.Nuke.Maui.sln --project-name AvantiPoint.Nuke.Maui'
        run: ./build.cmd CompileLib --solution AvantiPoint.Nuke.Maui.sln --project-name AvantiPoint.Nuke.Maui
      - name: Upload Artifact
        uses: actions/upload-artifact@v3
        with:
          name: nuget
          path: artifacts
  publish-internal:
    name: publish-internal
    needs: [compile-lib,android-build,ios-build,maccatalyst-build,windows-build]
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v2
        with:
          fetch-depth: 0
      - name: Download 'nuget'
        uses: actions/download-artifact@v3
        with:
          name: nuget
          path: artifacts
      - name: Cache ~/.nuget/packages
        uses: actions/cache@v2
        with:
          path: |
            ~/.nuget/packages
          key: publish-internal-${{ runner.os }}-${{ hashFiles('**/global.json', '**/*.csproj') }}
      - name: Run './build.cmd PublishNuGet'
        run: ./build.cmd PublishNuGet
        env:
          InHouseNugetFeed: ${{ secrets.IN_HOUSE_NUGET_FEED }}
          InHouseApiKey: ${{ secrets.IN_HOUSE_API_KEY }}
          CodeSignCertificate: ${{ secrets.CODESIGNCERTIFICATE }}
          CodeSignClientId: ${{ secrets.CODESIGNCLIENTID }}
          CodeSignClientSecret: ${{ secrets.CODESIGNCLIENTSECRET }}
          CodeSignKeyVault: ${{ secrets.CODESIGNKEYVAULT }}
          CodeSignTenantId: ${{ secrets.CODESIGNTENANTID }}
